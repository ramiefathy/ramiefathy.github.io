{
  "name": "ramie-fathy-personal-website",
  "version": "1.0.0",
  "description": "Personal website for Dr. Ramie Fathy - hosted on GitHub Pages",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/ramiefathy/ramiefathy.github.io"
  },
  "homepage": "https://ramiefathy.github.io",
  "scripts": {
    "serve": "python -m http.server 8000",
    "test": "echo \"GitHub Pages static hosting - no build process needed\" && exit 0",
    "site:dev": "npm --prefix site run dev",
    "site:build": "npm --prefix site run build",
    "site:preview": "npm --prefix site run preview",
    "site:test": "npm --prefix site run test",
    "site:test:watch": "npm --prefix site run test:watch",
    "site:test:e2e": "npm --prefix site run test:e2e",
    "site:test:e2e:update": "npm --prefix site run test:e2e:update",
    "clinic:scheduler:build": "babel site/public/apps/clinic-scheduler-pro/src/main.jsx --out-file site/public/apps/clinic-scheduler-pro/assets/main.js --presets @babel/preset-react && babel site/public/apps/clinic-scheduler-pro/src/main-animated.jsx --out-file site/public/apps/clinic-scheduler-pro/assets/main-animated.js --presets @babel/preset-react && rm -rf site/public/apps/clinic-scheduler-pro/assets/lib && mkdir -p site/public/apps/clinic-scheduler-pro/assets/lib && cp -R site/public/apps/clinic-scheduler-pro/src/lib/* site/public/apps/clinic-scheduler-pro/assets/lib/ 2>/dev/null || true",
    "test:unit": "echo 'Unit tests will be configured in Phase 3' && exit 0",
    "test:e2e": "npx firebase emulators:exec --only firestore,auth 'npx playwright test --reporter=list'",
    "perf:run": "npx firebase emulators:exec --only firestore 'node tests/perf/run-all.js'",
    "perf:seed": "npx firebase emulators:exec --only firestore 'node tests/perf/seed-large-institution.js'",
    "test:integration": "npx firebase emulators:exec --only firestore,auth 'node tests/integration/firebase-service.test.js'",
    "clinic:smoke": "npm run test:unit && npm run clinic:scheduler:build && npm test --prefix functions-backend && node functions-backend/verify-functions.js && python -m compileall services/ai-scribe",
    "backfill:members": "node functions-backend/scripts/backfill-members.js --dry-run",
    "backfill:members:apply": "node functions-backend/scripts/backfill-members.js",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "keywords": [
    "dermatology",
    "medical",
    "personal-website",
    "github-pages"
  ],
  "author": "Dr. Ramie Fathy",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.28.3",
    "@babel/core": "^7.28.4",
    "@babel/preset-react": "^7.27.1",
    "@playwright/test": "^1.46.1",
    "@testing-library/jest-dom": "^6.4.2",
    "@testing-library/react": "^14.2.1",
    "firebase-admin": "^12.0.0",
    "firebase-functions-test": "^3.4.1",
    "firebase-tools": "^13.8.0",
    "jsdom": "^24.0.0",
    "live-server": "^1.2.2",
    "turndown": "^7.2.1",
    "vitest": "^1.6.0",
    "esbuild-register": "^3.5.0"
  },
  "engines": {
    "node": "^18.19.0 || ^20.11.0"
  },
  "dependencies": {
    "@google-cloud/vertexai": "^1.10.0",
    "firebase": "^10.13.1"
  }
}
